# Variables de entorno para la base de datos
# Para desarrollo local (sin Docker)
DATABASE_URL_LOCAL=postgresql://pgadmin:secret@localhost:5436/freelance_db
# Para Docker (se usa automáticamente en docker-compose.yml)
DATABASE_URL=postgresql://pgadmin:secret@db:5432/freelance_db

# Puerto de la aplicación
PORT=3000

# Entorno
NODE_ENV=development 

# JWT Configuration - IMPORTANTE: Cambia este secreto por uno más seguro en producción
JWT_SECRET=tu_clave_super_secreta_para_jwt_freelance_2025_beto_branch
JWT_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=http://localhost:5173